# Copy this file to backend.hcl in this directory before initializing the stack:
# cp infra/backend/backend.hcl.example infra/backend/backend.hcl
#
# Replace the placeholders with outputs from the bootstrap stack:
# terraform -chdir=infra/bootstrap output
# The destination file is ignored by Git.

bucket         = "<state-bucket-name>"
key            = "backend/terraform.tfstate"
region         = "<aws-region>"
dynamodb_table = "<terraform-lock-table-name>"
encrypt        = true

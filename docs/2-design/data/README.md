# Data design

Logical and physical data model for RemindLedger.

| File | Purpose |
|------|---------|
| [logical-data-model.puml](logical-data-model.puml) | Conceptual entities and relationships (DB-agnostic). |

Physical schema (tables, DDL, migrations) will follow once the database is chosen; add files here as needed (e.g. `physical-schema.sql` or ER diagram).
